version: "3.8"
services:
  app:
    build: .
    container_name: untuk-papa
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
